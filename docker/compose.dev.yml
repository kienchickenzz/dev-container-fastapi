services:
    app:
        build:
            context: ..
            dockerfile: ./docker/dockerfile
            target: devcontainer
        env_file:
            - ../.env
        ports:
            - "8001:8000"

networks:
    default:
        name: detector_network
